extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.3", import: ["@key"])

type Query {
  allFlights: [Flight]
}

type Flight @key(fields: "id") {
  id: ID!
  origin: Airport
  destination: Airport
}

type Airport {
  airportCode: String!
}
